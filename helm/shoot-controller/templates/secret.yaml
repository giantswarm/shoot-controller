apiVersion: v1
kind: Secret
metadata:
  name: {{ include "resource.default.name" . }}-anthropic
  namespace: {{ include "resource.default.namespace" . }}
  labels:
  {{- include "labels.common" . | nindent 4 }}
type: Opaque
data:
  anthropicAPIKey: {{ .Values.anthropicAPIKey | b64enc | quote }}
